import { css } from "@emotion/react";

export const flexCenterLayerStyle = {
    wrap: css({
        background: 'var(--background-color)',
        height: '100%',
        width: '100%',
    }),
    
}